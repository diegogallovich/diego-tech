import styled from '@emotion/styled';

export const Container = styled.div`
  padding: 4rem;
`;

export const SloganText = styled.h2`
  font: var(--h2);
  color: var(--black);

  display: flex;
  flex-direction: column;
`;

export const ProblemSpan = styled.span`
  color: var(--problems);
`;

export const SolutionsSpan = styled.span`
  color: var(--solutions);
`;
